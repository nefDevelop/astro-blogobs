import type { FooterConfig } from "../types/config";

export const footerConfig: FooterConfig = {
  // Habilitar la función de inyección de HTML en el pie de página
  enable: false, // Habilitar la función de inyección de HTML en el pie de página
};

// Edita directamente el archivo config/FooterConfig.html para añadir contenido personalizado como el número de registro.
